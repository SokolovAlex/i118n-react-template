import sizes from './sizes';

const family = 'HelveticaNeue, HelveticaNeueCyr, Helvetica, Arial, sans-serif';
const size = '14px';

export default {
    base: family,
    size,
    family,
    button: `bold ${size}/${sizes.controls.height} ${family}`,
};